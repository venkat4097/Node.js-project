<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <!-- <link rel="stylesheet" href="/stylesheets/style.css"> -->
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
</head>
</head>

</head>
<body>
  
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white">
            <form action="/create" method="post">
                <input class="block w-full  px-5 py-2 bg-zinc-800 rounded-md outline-none" type="text" name="title" placeholder="title goes here">
                <textarea class="block w-full mt-2 px-5 py-2 bg-zinc-800 resize-none rounded-md outline-none" name="details" placeholder="write your task details"></textarea>
             <input class="mt-2 bg-blue-600 px-5 py-2 rounded-md" type="submit" value="create task">   
            </form>
            <div class="tasks  flex gap-3 flex-wrap py-10">
                <% if(files.length>0){ %>
                <% files.forEach(function(val){%>
                    <div class="task  w-72 px-3 py-4 bg-zinc-800 rounded-md">
                        <h1 class="text-3xl tracking-tighter"><%= val%></h1>
                        <div class="flex w-full justify-between items-center">
                            <a class="text-blue-500 inline-block mt-2" href="/file/<%= val%>">Read more</a>
                            <a class="text-green-200" href="/edit/<%= val %>">Edit Filename</a>
                            <a class="text-red-300" href="/delete/<%= val%>">
                                <i class="fas fa-trash"></i></a>
                        </div>
                        
                        </div>
                    <% }) %>
                    <%} else {%>
                        <h3 class="text-zinc-600">No taks yet</h3>
                        <%}%>
                
            </div>
        </div>
        

    </div>
</body>
</html>